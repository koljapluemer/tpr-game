class_name TopicData extends Resource
## Holds data regarding a `Topic`.
## Loosely connected with that class, which is more of a "topic template"
# via the `topic_name` prop that is a kind of primary key.

@export var topic_name:String
@export var points:=0
